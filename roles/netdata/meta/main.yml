---
galaxy_info:
  role_name: netdata
  author: Server Helper
  description: Deploy Netdata monitoring (child or parent mode)
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm]
  galaxy_tags: [monitoring, netdata, metrics]

allow_duplicates: true
dependencies: []
