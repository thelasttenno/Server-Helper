# Netdata Stack - System Monitoring
# Deployed via Dockge

version: '3.8'

services:
  netdata:
    image: netdata/netdata:latest
    container_name: netdata
    hostname: {{ hostname | default(ansible_hostname) }}
    restart: unless-stopped
    ports:
      - "{{ monitoring.netdata.port }}:19999"
    cap_add:
      - SYS_PTRACE
      - SYS_ADMIN
    security_opt:
      - apparmor:unconfined
    volumes:
      - netdataconfig:/etc/netdata
      - netdatalib:/var/lib/netdata
      - netdatacache:/var/cache/netdata
      - /etc/passwd:/host/etc/passwd:ro
      - /etc/group:/host/etc/group:ro
      - /proc:/host/proc:ro
      - /sys:/host/sys:ro
      - /etc/os-release:/host/etc/os-release:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - DOCKER_HOST=/var/run/docker.sock
{% if monitoring.netdata.claim_token | default('') | length > 0 %}
      - NETDATA_CLAIM_TOKEN={{ monitoring.netdata.claim_token }}
{% if monitoring.netdata.claim_rooms | default('') | length > 0 %}
      - NETDATA_CLAIM_ROOMS={{ monitoring.netdata.claim_rooms }}
{% endif %}
      - NETDATA_CLAIM_URL=https://app.netdata.cloud
{% endif %}
    networks:
      - monitoring

volumes:
  netdataconfig:
  netdatalib:
  netdatacache:

networks:
  monitoring:
    external: true
    name: monitoring
