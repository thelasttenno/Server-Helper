---
- name: Converge
  hosts: all
  become: true
  vars:
    target_domain: "test.local"
    control_dns:
      enabled: true
      pihole_port: 8053
      dns_port: 5353
    vault_pihole_password: "test-password"
  roles:
    - role: pihole
