[DEFAULT]
bantime = {{ security.fail2ban.sshd_bantime }}
findtime = 600
maxretry = {{ security.fail2ban.sshd_maxretry }}
destemail = root@localhost
sendername = Fail2Ban
action = %(action_mwl)s

[sshd]
enabled = true
port = ssh
logpath = %(sshd_log)s
backend = %(sshd_backend)s
maxretry = {{ security.fail2ban.sshd_maxretry }}
bantime = {{ security.fail2ban.sshd_bantime }}
