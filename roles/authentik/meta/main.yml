---
galaxy_info:
  role_name: authentik
  author: Server Helper
  description: Deploy Authentik SSO/OIDC with PostgreSQL and Redis
  license: MIT
  min_ansible_version: "2.14"
  platforms:
    - name: Ubuntu
      versions: [jammy, noble]
    - name: Debian
      versions: [bookworm]
  galaxy_tags: [sso, authentik, oidc, identity]
dependencies: []
