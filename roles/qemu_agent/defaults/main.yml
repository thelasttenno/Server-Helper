---
# QEMU Guest Agent role defaults

# Enable/disable QEMU guest agent
qemu_agent_enabled: "{{ target_virtualization.qemu_guest_agent | default(true) }}"

# Skip for non-VM environments
qemu_agent_skip: "{{ qemu_agent_skip | default(false) }}"
