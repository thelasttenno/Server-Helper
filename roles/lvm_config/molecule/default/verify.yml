---
- name: Verify
  hosts: all
  gather_facts: true
  tasks:
    - name: Verify role completed (skip mode)
      ansible.builtin.debug:
        msg: "LVM role completed in skip mode (no LVM in container)"
