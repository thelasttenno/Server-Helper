---
# =============================================================================
# add-target.yml — Add new server to fleet
# =============================================================================
# Bootstrap + deploy agents on a new server.
# Assumes the host is already in inventory/hosts.yml.
#
# Usage:
#   ansible-playbook playbooks/add-target.yml --limit new-server

- name: Bootstrap new target
  ansible.builtin.import_playbook: bootstrap.yml

- name: Deploy target roles
  ansible.builtin.import_playbook: target.yml
