---
# =============================================================================
# Server Helper v2.0 — Inventory
# =============================================================================
all:
  children:
    control:
      hosts:
        control-node:
          ansible_host: 192.168.1.10
          ansible_user: admin
    targets:
      hosts:
        # Add target hosts here
        # target-01:
        #   ansible_host: 192.168.1.11
        #   ansible_user: admin
        # target-02:
        #   ansible_host: 192.168.1.12
        #   ansible_user: admin
