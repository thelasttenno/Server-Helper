# Server Helper - Test Dependencies
# ==================================
# WARNING: Ubuntu 24.04+ blocks pip installs (PEP 668). Use pipx:
#   pipx install molecule
#   pipx inject molecule molecule-plugins[docker] pytest-testinfra
# Or use: make install-test-deps

# Molecule - Ansible role testing framework
molecule>=6.0.0
molecule-plugins[docker]>=23.0.0

# Ansible testing utilities
ansible-lint>=6.0.0
yamllint>=1.28.0

# Testing framework
pytest>=7.0.0
pytest-testinfra>=10.0.0

# Docker SDK for Python (required by molecule-docker)
docker>=6.0.0

# Additional utilities
jmespath>=1.0.0
